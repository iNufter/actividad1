#include <iostream>

int main() {
    float numero1, numero2;
    
    std::cout << "Escribe el primer numero: ";
    std::cin >> numero1;
    
    std::cout << "Escribe el segundo numero: ";
    std::cin >> numero2;
    
    float suma = numero1 + numero2;
    float resta = numero1 - numero2;
    float multiplicacion = numero1 * numero2;
    float division = numero2 != 0 ? numero1 / numero2 : 0; // Evitar división entre cero
    
    std::cout << "La suma es: " << suma << std::endl;
    std::cout << "La resta es: " << resta << std::endl;
    std::cout << "La multiplicacion es: " << multiplicacion << std::endl;
    
    if (numero2 != 0)
        std::cout << "La division es: " << division << std::endl;
    else
        std::cout << "La division no es posible (division entre cero)." << std::endl;
    
    return 0;
}
